import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {   //estilo do meu app
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'blue'
  }
})

  export default styles